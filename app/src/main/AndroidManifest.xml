<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.stefan.androidwebsockets">

    <uses-permission android:name="android.permission.INTERNET" />



    <application
        android:label="@string/app_name"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:name="SessionId"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity
        android:name=".ProjectActivity" android:label="@string/title_project_activity" >

        </activity>

        <activity android:name=".Subactivity" android:label="@string/title_sub_project_activity" android:parentActivityName="com.example.stefan.androidwebsockets.ProjectActivity" android:configChanges="orientation|screenSize">
        <meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value="com.example.stefan.androidwebsockets.ProjectActivity" >
        </meta-data>
        </activity>
    </application>

</manifest>
